# gitpod-blank-template
Default

Current problem :

There is no mysql, use php7.4, but looks enough for node.js/python web development IDK ?
You can then install manually some stuff needed to fit your requirements, but when you close the project/stop the instance, you'll need to re-install it again XD
